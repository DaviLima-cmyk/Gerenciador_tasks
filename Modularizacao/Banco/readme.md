# Todo o projeto é dividido em 3 packs


# cada pack é uma,CLASSE

# cada pack apresente um modulo em especifico que fica encarregado de carregar a classe base,que servira de heranca para os outros modulos

# Clientes(pack) guardara a classe Cliente,com nome .Alem disso ela carrega Class Pessoa,que ira herdar atributos da classe Cliente

# Contas(pack) guarda a classe base Conta e suas subclasses ContaCorrente,Conta Poupanca

# Operacoes guarda todos os tipos de operacoes realizaveis,como sacar,depositar,consultar saldo e historico

# alguns atributos e metodos estam encapsulados e requerem metodos especificos para serem ultilizados,por conta de politicas de segurança e consenso,no caso atributos privados

# ale disso a relacao entre a classe Historico e Conta e de composicao ja que uma nao existe sem a outra,e cliente e conta e agregacao visto que Conta(Geral) depende de um cliente
# para existir e cliente e Independente